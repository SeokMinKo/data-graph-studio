# Goal
Improve data-graph-studio daily evolution batch for Windows offline installer stability, runtime NameError/ImportError prevention, and UX recovery messaging.

# Scope
- Fix immediate F821 runtime-risk in table model.
- Add lint configuration to avoid scanning nested temporary worktrees during CI/local checks.
- Improve WPR conversion error guidance for offline Windows users.

# Constraints
- Small batches.
- Validate each batch (ruff F821 + py_compile + pytest subset).

# Definition of Done
- Targeted fixes committed.
- Required verification commands pass.

# Batches
1. Runtime NameError fix in table panel.
2. Lint scope hardening for repo/worktrees noise.
3. UX recovery guidance in WPR conversion step.

# Progress Log
- Started.

# Evidence
- Pending.

## Batch 1 Closeout
- Done: Fixed NameError risk in table_panel exception logging vars.
- Tests:
  - ruff check data_graph_studio/ui/panels/table_panel.py --select F821 (pass)
  - python3 -m py_compile data_graph_studio/ui/panels/table_panel.py (pass)
  - pytest -q tests/test_table_model.py (25 passed)
- Next: Harden lint scope and improve WPR recovery UX.

## Batch 2 Closeout
- Done: Added Ruff config to exclude nested `.worktrees` and build artifacts from lint scope.
- Tests:
  - ruff check . --select F821 (pass)
  - python3 -m py_compile data_graph_studio/ui/wizards/wpr_convert_step.py data_graph_studio/ui/panels/table_panel.py (pass)
  - pytest -q tests/test_ui_imports.py (62 passed)
- Next: Improve WPR failure recovery guidance.

## Batch 3 Closeout
- Done: Enhanced WPR convert step with FileNotFoundError handling and actionable recovery guide (Windows offline path guidance included).
- Tests:
  - ruff check data_graph_studio/ui/wizards/wpr_convert_step.py --select F821 (pass)
  - python3 -m py_compile data_graph_studio/ui/wizards/wpr_convert_step.py (pass)
  - pytest -q tests/test_wpr_convert_step.py (2 passed)
- Next: Commit and push.
