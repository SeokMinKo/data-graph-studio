# GOAT Rounds 12–25 Master Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development per round.

**Goal:** Push GOAT score from 7.97/10 → 9+/10 across 14 rounds of systematic improvement.

**Architecture:** Round-per-day execution. Each round: implement → merge → push → GOAT re-audit. Score tracked cumulatively.

**Current state (after Round 11):** 1647 tests, 47 `except Exception` in core/, score 7.97/10.

---

## Score Tracking

| Round | D (×1.5) | B (×1.5) | F (×1) | G (×1) | E (×1.5) | A (×1.5) | C (×1) | Total |
|---|---|---|---|---|---|---|---|---|
| R11 | 7.5 | 7.5 | 8.0 | 8.0 | 8.5 | 8.0 | 8.5 | 7.97 |
| R12 | ↑ | | | | | | | ? |
| … | | | | | | | | |
| R25 | 9+ | 9+ | 9+ | 9+ | 9+ | 9+ | 9 | 9+ |

---

## Phase 1: D — Error Handling (Rounds 12–16)
47 → 0 broad `except Exception`. Each round targets ~10 blocks in parallel task pairs.

### Round 12 — D batch 1 (10 blocks)
**Task 1 (parallel):** `export_workers.py` (3) + `comparison_algorithms.py` (2)
- `export_workers.py:138`: After `except ExportError`, catch `(OSError, MemoryError, pl.exceptions.InvalidOperationError, pl.exceptions.ComputeError, PermissionError)`
- `export_workers.py:292,312`: Cleanup-and-reraise blocks → `(OSError, MemoryError, pl.exceptions.InvalidOperationError, pl.exceptions.ComputeError, PermissionError)`
- `comparison_algorithms.py:48`: shapiro test fallback → `(ValueError, TypeError)`
- `comparison_algorithms.py:140`: normality test dict return → `(ValueError, TypeError, ArithmeticError)`

**Task 2 (parallel):** `dataset_manager.py` (2) + `profile_controller.py` (3)
- `dataset_manager.py:227`: callback dispatch → `(TypeError, RuntimeError, AttributeError)`
- `dataset_manager.py:450`: parallel load non-DatasetError → `(DataLoadError, OSError, MemoryError, pl.exceptions.InvalidOperationError)`
- `profile_controller.py:53`: GraphSettingMapper + state apply → `(TypeError, AttributeError, ValueError)`
- `profile_controller.py:155`: store.export_async → `(OSError, PermissionError, TypeError)`
- `profile_controller.py:176`: store.import_async + JSON → `(OSError, json.JSONDecodeError, ValueError, TypeError)`

### Round 13 — D batch 2 (8 blocks)
**Task 1:** `comparison_report_types.py` (3 at :104,:187,:190) + `comparison_report.py` (3 at :150,:383,:404)
- comparison_report_types serialization → `(TypeError, ValueError, AttributeError)`
- comparison_report wrapping → narrow to specific types being wrapped

**Task 2:** `data_engine.py:983` + `data_engine_analysis_mixin.py:238` + `data_query_helpers.py:116`
- data_engine:983 wrapping → specific Polars + OS types
- data_engine_analysis_mixin:238 → `(pl.exceptions.InvalidOperationError, ValueError, TypeError)`
- data_query_helpers:116 → `(pl.exceptions.InvalidOperationError, ValueError, TypeError)`

### Round 14 — D batch 3 (6 blocks)
**Task 1:** `expressions.py:181` + `expressions_parser.py:200` + `expression_engine.py:349`
- expressions eval → `(NameError, SyntaxError, TypeError, ValueError, ZeroDivisionError)`
- expressions_parser → `(SyntaxError, ValueError, TypeError)`
- expression_engine evaluate → `(pl.exceptions.InvalidOperationError, NameError, SyntaxError, TypeError, ValueError)`

**Task 2:** `statistics_timeseries.py:240` + `dashboard_layout.py:233` + `io_abstract.py:205`
- statistics_timeseries seasonal decompose → `(ValueError, TypeError, ArithmeticError)`
- dashboard_layout geometry → `(ValueError, TypeError, AttributeError)`
- io_abstract read → `(OSError, UnicodeDecodeError, ValueError)`

### Round 15 — D batch 4 (12 blocks)
**Task 1:** `ipc_server.py:206` + `annotation_controller.py:61` + `graph_setting_mapper.py` (3)
- ipc handler dispatch → `(TypeError, ValueError, AttributeError)`  (broad needed but log +re-raise)
- annotation_controller → `(ValidationError, TypeError, ValueError)`
- graph_setting_mapper:81,:163,:179 → `(TypeError, ValueError, AttributeError, KeyError)`

**Task 2:** `file_watcher.py` (2) + `updater.py` (2) + `file_loader.py:67` + `file_loader_formats.py:188`
- file_watcher:169,409 → `(OSError, PermissionError)`
- updater:35,42 → `(OSError, requests.RequestException, json.JSONDecodeError)` or similar
- file_loader:67 → `(UnicodeDecodeError, ValueError)`
- file_loader_formats:188 → `(pl.exceptions.InvalidOperationError, OSError, ValueError)`

### Round 16 — D batch 5 + targeted tests (5 blocks + tests)
**Task 1:** `export_controller.py` (2) + `etl_helpers.py` (2) + `file_loader_formats.py` remaining
- export_controller:227,261 → `(OSError, PermissionError, ExportError, pl.exceptions.InvalidOperationError)`
- etl_helpers:203,221 → narrowed to specific Polars + type errors

**Task 2:** Tests for rounds 12-15 narrowed paths
- New test file `test_except_narrowing_r12_r15.py` with targeted error-case tests
- Verify each narrowed block is exercised

---

## Phase 2: B — Spec/Contracts (Rounds 17–18)
Bring `file_loader.py`, `ipc_server.py`, `dataset_manager.py` to GOAT format docstrings.

### Round 17 — B: file_loader.py docstrings (top 20 methods)
**Task 1:** GOAT docstrings for load/format methods
**Task 2:** GOAT docstrings for detect/helper methods

### Round 18 — B: ipc_server.py + dataset_manager.py
**Task 1:** ipc_server.py public methods (connect, send_command, register_handler, dispatch methods)
**Task 2:** dataset_manager.py remaining uncovered public methods

---

## Phase 3: F — Changeability (Round 19)

### Round 19 — F: Constants centralization
**Task 1:** Move from `file_loader.py` to `constants.py`:
- `PARQUET_CONVERT_THRESHOLD`, `WINDOWED_LOAD_THRESHOLD`, `DEFAULT_ENCODING`, `ENCODING_SAMPLE_SIZE`
- `DEFAULT_CHUNK_SIZE`, `MAX_RETRIES`, `LAZY_EVAL_THRESHOLD`, `DELIMITER_SAMPLE_LINES`

**Task 2:** Fix inline magic numbers:
- `export_workers.py` progress values `10`, `50`, `80`, `90`, `95` → named constants
- `streaming_controller.py:57` `1000` → `DEFAULT_POLL_INTERVAL_MS`
- `data_query.py` `limit=1000`, `n=10000` → constants
- `report_types.py` `chart_dpi=150` → constant

---

## Phase 4: G — Observability (Round 20)

### Round 20 — G: timed_operation + structured logging
**Task 1:** Add `timed_operation` to:
- `comparison_engine.perform_statistical_test`
- `statistics_timeseries` seasonal/trend decompose
- `expression_engine.evaluate`
- `export_controller` export dispatch

**Task 2:** Convert unstructured logger calls in:
- `file_loader.py` (top offender — 15+ `%s` style)
- `dataset_manager.py` (5+ unstructured)
- `file_watcher.py` (8+ unstructured)

---

## Phase 5: E — Tests (Rounds 21–22)

### Round 21 — E: Narrowed-except test coverage
**Task 1:** Tests for R12–R13 narrowed blocks
**Task 2:** Tests for R14–R16 narrowed blocks

### Round 22 — E: Advanced tests
**Task 1:** Contract tests for IPC protocol (updated for R15 changes)
**Task 2:** Property-based tests for expressions engine invariants

---

## Phase 6: A + C (Rounds 23–24)

### Round 23 — A: Structure improvements
**Task 1:** DataEngine constructor accepts optional FileLoader, DataQuery (injectable for tests)
**Task 2:** Remaining circular import audit + fix

### Round 24 — C: Type improvements
**Task 1:** `NewType("DatasetId", str)` + `NewType("SchemeId", str)` throughout
**Task 2:** Value objects for threshold sizes (`FileSizeBytes = NewType(...)`)

---

## Round 25 — Final Polish + Comprehensive Audit

**Task 1:** Any lingering issues from previous rounds
**Task 2:** Verified comprehensive GOAT re-audit → target 9+/10
